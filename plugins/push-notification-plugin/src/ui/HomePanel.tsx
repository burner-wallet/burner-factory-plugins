import React, { Fragment } from 'react';
import { PluginElementContext } from '@burner-wallet/types';

const HomePanel: React.FC<PluginElementContext> = ({ plugin }) => {
  return (
    <Fragment />
  );
}

export default HomePanel;
